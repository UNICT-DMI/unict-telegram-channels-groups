import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'UNICT-Telegram-Channels-Groups',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
